{
  "name": "enterprise-rag-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Enterprise RAG Platform Monorepo",
  "workspaces": [
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "dev": "docker-compose up --build",
    "install:all": "npm install",
    "clean": "rimraf node_modules packages/*/node_modules services/*/node_modules",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "rimraf": "^5.0.5",
    "eslint": "^8.56.0",
    "nodemon": "^3.0.2",
    "prettier": "^3.1.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}